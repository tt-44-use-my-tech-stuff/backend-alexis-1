const db = require('../data/db-config'); //eslint-disable-line

const findAll = () => {

}

const findBy = filter => { //eslint-disable-line

}

const findById = rental_id => { //eslint-disable-line

}

const create = tech_item => { //eslint-disable-line

}

const update = (rental_id, changes) => { //eslint-disable-line

}

const deleteById = rental_id => { //eslint-disable-line

}

module.exports = {
  findAll,
  findBy,
  findById,
  create,
  update,
  deleteById
}